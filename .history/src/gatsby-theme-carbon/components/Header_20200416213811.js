import React from "react";
import Header from "gatsby-theme-carbon/src/components/Header";

const ShadowedCustomHeader = (props) => <Header {...props}></Header>;

export default ShadowedCustomHeader;
